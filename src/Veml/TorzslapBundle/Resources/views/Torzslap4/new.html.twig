{% extends 'VemlHelloBundle::base.html.twig' %}

{% block body %}
<h1>Torzslap4 creation</h1>

<form action="{{ path('4_sz_torzslap_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('4_sz_torzslap') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}